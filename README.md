🏡 Property Listing Renderer

A simple JavaScript project that dynamically renders property cards on a webpage using property data arrays. Each property displays its image, location, price in GBP, a comment/description, and the total room area (in m²) calculated from an array of room sizes.

📌 Features

Dynamically generates property cards using JavaScript.

Imports property data from external JS modules.

Calculates total room size using Array.reduce().

Supports placeholder data if property array is missing.

Clean and reusable function getPropertyHtml().

💡 Customization

You can:

Add more property fields in the object.

Change the card layout using CSS.

Add filtering or sorting features.

Replace the placeholder image and data.

📜 License

This project is free to use and modify.